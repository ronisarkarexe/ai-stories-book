export enum tagTypes {
  user = "user",
  otp = "otp",
  model = "model",
  post = "post",
  comment = "comment",
}

export const tagTypesList: string[] = [
  tagTypes.user,
  tagTypes.otp,
  tagTypes.model,
  tagTypes.post,
  tagTypes.comment,
];
